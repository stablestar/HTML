<style>
   {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background: #f2f2f2;
    margin: 0;
    font-family: Arial, sans-serif;
  }

  .phone {
    width: 300px;
    height: 600px;
    background: #111;
    border-radius: 30px;
    padding: 20px;
    position: relative;
    box-shadow: 0 0 20px rgba(0,0,0,0.5);
  }

  .screen {
    background: #fff;
    width: 100%;
    height: 100%;
    border-radius: 20px;
    position: relative;
    overflow: hidden;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
  }

  .message, .response {
    padding: 12px 16px;
    border-radius: 20px;
    max-width: 80%;
    margin-bottom: 10px;
    opacity: 0;
    transform: translateY(20px);
  }

  .message {
    background: #e0e0e0;
    align-self: flex-start;
    position: relative;
  }

  .response {
    background: #bb9669;
    color:white;
    align-self: flex-end;
    position: relative;
  }

  .message::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 20px;
    width: 0;
    height: 0;
    border: 8px solid transparent;
    border-top-color: #e0e0e0;
    transform: translateY(100%);
  }

  .response::after {
    content: "";
    position: absolute;
    bottom: 0;
    right: 20px;
    width: 0;
    height: 0;
    border: 8px solid transparent;
    border-top-color: #d1f7c4;
    transform: translateY(100%);
  }

  /* Animations */
  .message:nth-of-type(1) { animation: popUp 1s forwards 1s; }
  .response:nth-of-type(2) { animation: popUp 1s forwards 2s; }
  .message:nth-of-type(3) { animation: popUp 1s forwards 3s; }
  .response:nth-of-type(4) { animation: popUp 1s forwards 4s; }

  @keyframes popUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
</head>
<body>

<div class="phone">
  <div class="screen">
    <div class="message">Who are you</div>
    <div class="response">We are TS Mortgage & Protection Solutions and we help people like you achieve their goals </div>
    <div class="message">Amazing, how do I get started?</div>
    <div class="response">Super easy! Just hit our contact page and we’ll guide you →</div>
  </div>
</div>

</body>
</html>
